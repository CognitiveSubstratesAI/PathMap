name = "PathMaps"
uuid = "a8b3d4e5-f012-4567-89ab-cdef01234567"
version = "0.3.0"
authors = ["PRIMUS Team"]

[deps]
Mmap = "a63ad114-7e13-5084-954f-fe012c677804"
PrecompileTools = "aea7be01-6a6a-4083-8856-8a6e6704d82a"
Zlib_jll = "83775a58-1f1d-513f-b197-d71354ab007a"

[compat]
Mmap = "1.11"
PrecompileTools = "1.3.3"
Zlib_jll = "1.3.1"
julia = "1.12"

# ⚠️ DEV/ANALYSIS TOOLS COME FROM THE GLOBAL env (@v1.12), not from here — see the same note in
# MORK/Project.toml. `tools/run_tests.sh` runs `julia --project=.`, whose LOAD_PATH includes
# "@v#.#", and nothing in this tree uses `Pkg.test` (the only consumer of [extras]/[targets]).
# ⚠️ `ProfileView` REMOVED 2026-08-21 — a vestige: it was in [extras] but never in [targets],
# and the note above already states tools come from the global env.
[extras]
Test = "8dfed614-e22c-5e08-85e1-65c5234f0b40"

[targets]
test = ["Test"]
